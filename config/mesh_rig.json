{
  "version": 2,
  "renderer": "webgl-articulated",
  "grid": { "columns": 24, "rows": 28 },
  "fallback": "canvas-raster",
  "assetStrategy": "runtime-extracted-arm-layers + emotion-aware-face-overlays + source-fallback",
  "articulatedSources": ["stand", "jump", "peace"],
  "faceRig": {
    "underpaintSourceFeatures": true,
    "emotionSpecificAnchors": true,
    "visemeSafeBounds": true,
    "expressionChannelTiming": true
  },
  "notes": "v0.3.0-alpha.3 splits conservative arm masks from flattened source sprites at runtime, rotates those layers around shoulder pivots, and repaints facial source features before emotion/viseme overlays to prevent double mouths and eyes. Unconfigured source poses retain the original raster fallback path."
}