{
  "version": 1,
  "renderer": "webgl-mesh",
  "grid": { "columns": 24, "rows": 28 },
  "fallback": "canvas-raster",
  "assetStrategy": "flattened-source-now, layered-textures-later",
  "notes": "The alpha branch deforms the existing transparent source PNG on a regional grid. Future separated PSD/PNG layers can reuse the same parameter and spring system."
}
